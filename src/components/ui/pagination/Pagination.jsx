// Phân trang

import styles from './pagination.module.css';
const Pagination = () => {
    return (
        <div>
            <button>Previous</button>
            <button>Next</button>
        </div>
    );
};

export default Pagination;
